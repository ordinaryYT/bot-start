{
  "name": "sms-sender",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev", // For local development
    "build": "echo 'No build process required'" // No build process required
  },
  "dependencies": {
    "sib-api-v3-sdk": "^8.2.0"
  }
}
